"use client";
import { excludeNavRoutes } from "@/utils/constants";
import React from "react";
import { usePathname } from "next/navigation";

const TopbarWrapper = () => {
  // Topbar disabled for modern dark theme design
  return null;
};

export default TopbarWrapper;
